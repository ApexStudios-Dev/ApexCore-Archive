{
    "required": true,
    "package": "${APEX_GROUP}.${MOD_ID}.fabric.mixin",
    "mixins": [
        "ExplosionMixin"
    ],
    "client": [ ],
    "server": [ ],
    "minVersion": "${MIXIN_VERSION}"
}
