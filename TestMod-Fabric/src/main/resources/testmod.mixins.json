{
    "required": true,
    "package": "${APEX_GROUP}.${TEST_MOD_ID}.fabric.mixin",
    "mixins": [ ],
    "client": [ ],
    "server": [ ],
    "minVersion": "${MIXIN_VERSION}",
    "refmap": "${TEST_MOD_ID}-refmap.json"
}
