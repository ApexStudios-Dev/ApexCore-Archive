{
    "required": true,
    "package": "${APEX_GROUP}.${MOD_ID}.shared.mixin",
    "mixins": [
        "MixinBlockStateBase",
        "MixinFireBlock",
        "accessors.AccessorBlock",
        "accessors.AccessorBlockEntityType",
        "invoker.InvokerBlock"
    ],
    "client": [ ],
    "server": [ ],
    "minVersion": "${MIXIN_VERSION}"
}
