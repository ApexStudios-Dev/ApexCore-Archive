{
    "required": true,
    "package": "${APEX_GROUP}.${MOD_ID}.shared.mixin",
    "mixins": [
        "MixinExplosion", "MixinItem"
    ],
    "client": [ ],
    "server": [ ],
    "minVersion": "${MIXIN_VERSION}"
}
